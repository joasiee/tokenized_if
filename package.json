{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "ts:build": "tsc --build",
    "ts:clean": "tsc --build --clean",
    "build": "yarn ts:clean && yarn ts:build",
    "start": "yarn workspace tokenized_if start",
    "postinstall": "update-ts-references"
  },
  "devDependencies": {
    "typescript": "^4.2.3",
    "update-ts-references": "^2.0.0"
  }
}
