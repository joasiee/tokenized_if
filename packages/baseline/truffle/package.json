{
  "name": "@tokenized_if/truffle",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "compile": "yarn truffle compile",
    "generate:types": "yarn typechain --target ethers-v5 --outDir ../dist/typechain '../dist/artifacts/*.json'",
    "build": "yarn compile && yarn generate:types"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.1.0",
    "truffle": "^5.2.5"
  },
  "devDependencies": {
    "@typechain/ethers-v5": "^6.0.5",
    "typechain": "^4.0.3"
  }
}
